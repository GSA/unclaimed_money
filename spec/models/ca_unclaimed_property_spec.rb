require 'spec_helper'

describe CaUnclaimedProperty do
  
  describe "get_name_data" do
    it "titleizes the name" do
      
    end
    
    it "returns the last name when there is only one word in the name column" do
    
    end
  
    it "returns the last name and first name in 'Lastname, Firstname' format when there are two words in the name column" do
    end

    it "returns the last name, first name, middle initial in 'Lastname, Firstname M' format when there are three words in the name column" do

    end

  end
end
